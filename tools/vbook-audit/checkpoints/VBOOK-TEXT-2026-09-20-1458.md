# VBook Text Audit Checkpoint — 2026-09-20 14:58 +07

## Current state

Physical VBook gate is in progress on Nokia C30, VBook 1.1.56. No candidate has been promoted to production yet.

### Main / candidate registry work
- Fixed target workflow earlier to pass `VBOOK_META_SOURCE=tools/vbook-audit/vbook171_meta.json`.
- Target benchmark across all 13 pinned candidates: ENGINE 13/13 PASS; SEARCH_IDENTITY 10/13 PASS.
- SEARCH failures remain: `lwxs`, `fuxsb`, `Novel Next`.
- Wattpad candidate v4 passes but production v3 already passes; do not promote v4 absent an independent reason.
- Temporary physical candidate registries were added. Latest isolated registry is `p2.json`.
- `p2.json` uses catalog-unique `[CAND]` names / temporary source identities only to defeat VBook registry dedupe. The ZIP paths remain the exact candidate packages; ZIP-internal metadata remains the real source/version.
- Candidate ZIP metadata was verified directly:
  - TruyệnCV V2 — https://metruyencv.com
  - TruyenNet V2 — https://truyennet.org
  - Đọc Truyện 14 V9 — https://doctruyen14.biz/
  - Truyện Zhihu Tổng Hợp V5 — https://www.truyendexuat.com
  - nhatruyen V9 — https://nhatruyen.site

### Physical registry proof
- `p2.json` add through `vbook://open?act=add-repo&url=...` returned success.
- VBook Library visibly rendered all 5 independent entries:
  - `[CAND] TruyệnCV V2`
  - `[CAND] TruyenNet V2`
  - `[CAND] Đọc Truyện 14 V9`
  - `[CAND] Truyện Zhihu Tổng Hợp V5`
  - `[CAND] nhatruyen V9`
- Installed count changed from 17 to 19 during candidate work.

### Exact installed-version proof obtained
- Long-press extension details on Nokia proves:
  - `Đọc Truyện 14` = **V9**
  - author `B`
  - source `doctruyen14.biz`
  - description contains `[Louis search identity fix]`
- Therefore the exact Đọc Truyện 14 V9 candidate ZIP is installed on-device.
- Do NOT call reader PASS yet.

### Physical source-picker blocker for Đọc Truyện 14
- Main Discovery/source picker currently shows sources beginning with TruyenNet/Truyện CV etc., but not Đọc Truyện 14.
- Đọc Truyện 14 is marked `18+`/NSFW in installed details.
- APK reverse findings:
  - source picker class: `com.vbook.app.ui.discovery.listplugins.ListExtensionDialog`
  - presenter implementation: obfuscated `LEb0`
  - `ListExtensionDialog` itself does not filter NSFW; it calls `LEN.e()` -> DAO `rN.z()`.
  - APK contains preference key `extension.filter.nsfw`.
  - XREF for that key is in class `Le5`, methods `T()Z` (getter) and `x0(Z)V` (setter).
  - regular Settings UI exposes `Làm mờ ảnh bìa truyện nsfw`, but no visible extension-filter toggle.
- Current status for Đọc Truyện 14: `PHYSICAL_DEFER_FILTER`, not parser fail and not reader PASS.

### Next exact actions when Nokia/VPS reconnects
1. Inspect `Le5.T()` and `Le5.x0(Z)` to determine the current preference semantics / where the filter is toggled, or otherwise expose the exact installed V9 source in source picker without modifying the extension ZIP.
2. Complete Đọc Truyện 14 V9 physical path: source picker -> real book -> detail -> TOC -> real chapter -> reader stable -> PASS_READER.
3. Verify installed versions for TruyenNet and TruyệnCV (both currently visible near top of installed list; installed count 19 suggests at least one other candidate may already have been consumed). Do not assume which one without long-press version proof.
4. Install/verify remaining candidates from `p2.json`: TruyệnCV V2, TruyenNet V2, Zhihu V5, nhatruyen V9.
5. Run physical reader gate for each exact candidate.
6. Restore normal registry set / remove temporary physical candidate registry artifacts from device.
7. Promote only physical PASS VN candidates plus already ENGINE+SEARCH-passing Chinese candidates; do not promote Wattpad v4, lwxs v3, fuxsb v8, Novel Next v7.
8. Run full `VBook Text Novel Acceptance` regression after promotion.

## Known code/reverse facts
- Deep link routes in VBook 1.1.56:
  - `vbook://open?act=add-repo&url=...` works.
  - `vbook://open?act=install-ext&url=...` reaches `N3()` which is a no-op in this build.
- `ExtensionTestService` exists but is `exported=false`.
- app `allowBackup=false`; Room DB cannot be pulled with normal ADB backup/run-as.
- bundled registries cannot be removed through normal UI; exact version proof + candidate catalog isolation is therefore required.

## Safety / fidelity rule
Never count an installed source as physical PASS unless the exact candidate version is proven on-device and the reader path is exercised. Engine PASS or a readable old version is insufficient.
