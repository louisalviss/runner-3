name: Reader Audio Ownership Audit

on:
  workflow_dispatch:
  push:
    branches:
      - reader-audio-core-integration-v33
    paths:
      - 'cloudflare/runner3-core/artifact-library-reader-v*-entry.js'
      - 'scripts/audit_reader_audio_owners.mjs'
      - '.github/workflows/reader-audio-owner-audit.yml'

permissions:
  contents: read

jobs:
  owner-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/audit_reader_audio_owners.mjs
