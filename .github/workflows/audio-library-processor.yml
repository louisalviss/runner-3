name: Audio Library Legacy Processor Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy extract/translate/TTS pipeline is disabled.'
          echo 'Audio must be authored by ChatGPT and enter through the encrypted ChatGPT TTS outbox.'
