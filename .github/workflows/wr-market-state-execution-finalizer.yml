name: WR Market State Execution Finalizer (RETIRED)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired contaminated Market-State finalizer
        run: |
          echo "RETIRED: this workflow depended on the invalidated leaked HGB q70 selector."
          echo "Do not use runs 32533359788, 32542005535, or 32566943369 as strategy evidence."
          echo "Any future execution finalizer must require a new causal PASS lineage and explicit provenance matching."
