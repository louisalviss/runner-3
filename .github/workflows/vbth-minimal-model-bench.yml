name: VBTH Minimal Model Benchmark
on:
  push:
    paths:
      - '.github/workflows/vbth-minimal-model-bench.yml'
permissions:
  contents: write
jobs:
  bench:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    env:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run frontier
        run: python vbth_bench/final_frontier.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add vbth_bench/results_v5
          git diff --cached --quiet && exit 0
          git commit -m "VBTH final cost-quality frontier results [skip ci]"
          git push
# trigger-v5
