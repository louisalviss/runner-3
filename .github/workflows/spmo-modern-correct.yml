name: SPMO Modern HF Corrected

on:
  push:
    branches: [main]
    paths:
      - 'spmo-modern2/correct_run.py'
      - '.github/workflows/spmo-modern-correct.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install duckdb pandas numpy requests yfinance
      - name: Run corrected modern SPMO reconstruction
        run: python -u spmo-modern2/correct_run.py
      - name: Upload corrected output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: spmo-modern-correct-${{ github.run_id }}
          path: spmo-modern2/output/
          if-no-files-found: error
          retention-days: 7
