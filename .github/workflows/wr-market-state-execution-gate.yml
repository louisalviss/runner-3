name: WR Market State Execution Gate (RETIRED)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired contaminated Market-State execution gate
        run: |
          echo "RETIRED: this gate joined executable rows to the invalidated leaked q70 selector from run 32542005535."
          echo "Execution collection code can be reused as infrastructure, but no old selected-set PnL/PF/DD is valid evidence."
          echo "A replacement gate must consume a new causal selector artifact and hard-check provenance before evaluation."
