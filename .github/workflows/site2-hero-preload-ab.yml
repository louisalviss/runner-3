name: Site2 Hero Preload A/B

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired candidate notice
        run: |
          echo "Candidate site2-organic-store-hero-preload-v1 is retired from automatic A/B execution."
          echo "Its canonical comparable decision is ROLLBACK."
          echo "A later automatic rerun produced a confounded bimodal result during origin warm/cold investigation and must not reactivate the candidate automatically."
