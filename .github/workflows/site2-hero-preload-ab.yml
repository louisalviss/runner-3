name: Site2 Hero Preload A/B

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'A/B workflow paused while Organic Store guards are updated.'
