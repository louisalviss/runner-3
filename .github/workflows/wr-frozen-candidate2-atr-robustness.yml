name: WR Frozen Candidate2 ATR Robustness
on:
  pull_request:
    branches: [main]
    paths:
      - 'wave-rider-verify/wr_frozen_candidate2_atr_robustness.py'
      - '.github/workflows/wr-frozen-candidate2-atr-robustness.yml'
permissions:
  actions: read
  contents: read
jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: wr2024-frozen-rolling-oos-final
          path: /tmp/a24
          github-token: ${{ github.token }}
          repository: louisalviss/runner-3
          run-id: 32253319968
      - uses: actions/download-artifact@v4
        with:
          name: wr2515-phase2-zonec10-final
          path: /tmp/a25
          github-token: ${{ github.token }}
          repository: louisalviss/runner-3
          run-id: 32233507091
      - name: Run frozen candidate2 ATR robustness
        run: python3 wave-rider-verify/wr_frozen_candidate2_atr_robustness.py
      - uses: actions/upload-artifact@v4
        with:
          name: wr-frozen-candidate2-atr-robustness-final
          path: /tmp/final5
          retention-days: 14
