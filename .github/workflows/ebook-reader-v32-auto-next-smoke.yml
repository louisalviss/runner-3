name: Ebook Reader v32 Auto Next Smoke (Legacy, No Deploy)

on:
  workflow_dispatch:

permissions:
  contents: read

# Archived legacy proof only. This workflow must never deploy runner3-core.
# Production deploy ownership belongs exclusively to
# runner3-core-public-hosted-reader-deploy.yml.
jobs:
  legacy-no-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'LEGACY_READER_V32_NO_DEPLOY=PASS'
          echo 'Production deploy owner: runner3-core-public-hosted-reader-deploy.yml'
