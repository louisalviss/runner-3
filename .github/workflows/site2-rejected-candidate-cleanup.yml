name: Site2 Rejected Candidate Cleanup Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired duplicate cleanup workflow. Use Site2 Candidate Force Off if cleanup is ever needed again."
