# Temporary validation branch disabled; no result files were persisted.
name: Core Edge Decompose Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
