# Ephemeral PR trigger for PIT quality validation; no result files are persisted.
name: Core Edge Decompose

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
