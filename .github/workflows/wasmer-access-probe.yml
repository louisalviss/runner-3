# PR observability probe
name: Wasmer access probe

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/wasmer-access-probe.yml
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/wasmer-access-probe.yml

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      WASMER_TOKEN: ${{ secrets.WASMER_TOKEN }}
    steps:
      - name: Check secret is available
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "${WASMER_TOKEN:-}" ]]; then
            echo "WASMER_TOKEN is missing or empty"
            exit 2
          fi
          echo "WASMER_TOKEN is present"

      - name: Network diagnostics
        shell: bash
        run: |
          set +e
          echo "DNS:"
          getent ahosts registry.wasmer.io | head -10
          echo "wasmer.io HTTP:"
          curl -4 -sS -o /dev/null -w '%{http_code} %{time_total}\n' --connect-timeout 8 --max-time 15 https://wasmer.io/
          echo "registry GraphQL HTTP:"
          curl -4 -sS -o /dev/null -w '%{http_code} %{time_total}\n' --connect-timeout 8 --max-time 15 https://registry.wasmer.io/graphql
          exit 0

      - name: Install Wasmer CLI
        shell: bash
        run: |
          set -euo pipefail
          curl https://get.wasmer.io -sSfL | sh
          echo "$HOME/.wasmer/bin" >> "$GITHUB_PATH"

      - name: Login non-interactively with Actions secret
        shell: bash
        run: |
          set -euo pipefail
          wasmer login --no-browser

      - name: Verify Wasmer account and list apps
        shell: bash
        run: |
          set -euo pipefail
          wasmer --version
          wasmer whoami
          wasmer app list
