name: Private Backtest Cost Sensitivity

on:
  push:
    branches: [main]
    paths:
      - 'jobs/private-backtest-costs.json'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: private-backtest-cost-sensitivity
  cancel-in-progress: false

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      RUNNER3_CORE_URL: https://runner3-core.ducduy2411.workers.dev
      RUNNER3_CORE_TOKEN: ${{ secrets.RUNNER3_CORE_TOKEN }}
      RUNNER3_SOURCE: runner-3-github-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Analyze execution cost sensitivity
        shell: bash
        run: |
          SCOPE="$(python -c 'import json; print(json.load(open("jobs/private-backtest-costs.json"))["scope"])')"
          python tools/private_backtest_cost_sensitivity.py --scope "$SCOPE"
