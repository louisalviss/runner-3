name: SPMO Complete Submission Probe
on:
  push:
    branches: [main]
    paths:
      - 'spmo-legacy/complete_probe.py'
      - '.github/workflows/spmo-complete-probe.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests
      - run: python -u spmo-legacy/complete_probe.py
