name: WR Alt Regime Interaction
on:
  pull_request:
    branches: [main]
    paths:
      - 'wave-rider-verify/wr_alt_regime_interaction.py'
      - '.github/workflows/wr-alt-regime-interaction.yml'
permissions:
  actions: read
  contents: read
jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: wr2024-frozen-rolling-oos-final
          path: /tmp/a24
          github-token: ${{ github.token }}
          repository: louisalviss/runner-3
          run-id: 32253319968
      - uses: actions/download-artifact@v4
        with:
          name: wr2515-phase2-zonec10-final
          path: /tmp/a25
          github-token: ${{ github.token }}
          repository: louisalviss/runner-3
          run-id: 32233507091
      - name: Run altcoin regime interaction diagnostic
        run: python3 wave-rider-verify/wr_alt_regime_interaction.py
      - uses: actions/upload-artifact@v4
        with:
          name: wr-alt-regime-interaction-final
          path: /tmp/final4
          retention-days: 14
