name: Runner5 Restore Lab Seed

on:
  push:
    branches: [main]
    paths:
      - 'restore-lab/requests/seed-runner5-restore-lab-1.json'
      - '.github/workflows/runner5-restore-lab-seed.yml'
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - 'restore-lab/requests/seed-runner5-restore-lab-1.json'
      - '.github/workflows/runner5-restore-lab-seed.yml'

permissions:
  contents: write

concurrency:
  group: runner5-restore-lab-seed-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  seed:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      AUTOMATION_KEY: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Decrypt Wasmer account state
        shell: bash
        run: |
          set -euo pipefail
          test -n "${AUTOMATION_KEY:-}" || { echo 'Automation encryption key unavailable'; exit 3; }
          echo '::add-mask::'"$AUTOMATION_KEY"
          openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 \
            -pass env:AUTOMATION_KEY \
            -in ops/wasmer/state.automation.aes \
            -out /tmp/wasmer-account.json
          chmod 600 /tmp/wasmer-account.json

      - name: Download official free theme and importer
        shell: bash
        run: |
          set -euo pipefail
          curl -fL --retry 3 --retry-delay 2 https://downloads.wordpress.org/theme/inspiro.zip -o /tmp/inspiro.zip
          curl -fL --retry 3 --retry-delay 2 https://downloads.wordpress.org/plugin/inspiro-starter-sites.zip -o /tmp/inspiro-starter-sites.zip
          test -s /tmp/inspiro.zip
          test -s /tmp/inspiro-starter-sites.zip

      - name: Install browser automation
        run: npm install --no-save playwright-core@1.58.2

      - name: Seed demo site
        run: node scripts/seed-runner5-restore-lab.mjs

      - name: Persist safe seed status
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          status=/tmp/runner5-restore-lab-seed.json
          if [ ! -s "$status" ]; then
            printf '{"status":"RUNNER_ERROR","siteSlug":"runner5-restore-lab-1","detail":"seed_status_missing"}\n' > "$status"
          fi
          rm -f /tmp/wasmer-account.json /tmp/inspiro.zip /tmp/inspiro-starter-sites.zip
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          for attempt in 1 2 3 4; do
            git fetch origin main
            git reset --hard origin/main
            mkdir -p ops/restore-lab
            cp "$status" ops/restore-lab/runner5-restore-lab-1.seed.json
            git add ops/restore-lab/runner5-restore-lab-1.seed.json
            if git diff --cached --quiet; then exit 0; fi
            git commit -m 'Persist Runner5 restore lab seed status'
            if git push origin HEAD:main; then exit 0; fi
            sleep 3
          done
          exit 5
