import app from "./artifact-library-reader-v6-entry.js";

const ROBOTS = "noindex, nofollow, noarchive, nosnippet, noimageindex";

function patchContinuousAudio(html) {
  if (!html.includes('id="viewer"') || html.includes('data-r3-audio-follow-v8="1"')) return html;

  const navNeedle = `function pagePrev(){if(rendition)rendition.prev();hideControls();}\n  function pageNext(){if(rendition)rendition.next();hideControls();}`;
  const navPatch = `${navNeedle}\n  window.r3ReaderBridge={\n    display(target){try{return rendition?rendition.display(target):Promise.resolve(false);}catch{return Promise.resolve(false);}},\n    next(){try{return rendition?rendition.next():Promise.resolve(false);}catch{return Promise.resolve(false);}},\n    prev(){try{return rendition?rendition.prev():Promise.resolve(false);}catch{return Promise.resolve(false);}},\n    current(){try{return rendition&&rendition.currentLocation?rendition.currentLocation():null;}catch{return null;}},\n    contents(){try{return rendition&&rendition.getContents?rendition.getContents()||[]:[];}catch{return [];}},\n    cfiFromNode(node){\n      try{\n        const contents=rendition&&rendition.getContents?rendition.getContents()||[]:[];\n        for(const content of contents){\n          try{\n            const doc=content&&content.document;\n            if(doc&&node&&doc.contains(node)&&typeof content.cfiFromNode==='function')return content.cfiFromNode(node);\n          }catch{}\n        }\n      }catch{}\n      return null;\n    },\n    persist(){\n      try{\n        const loc=rendition&&rendition.currentLocation?rendition.currentLocation():null;\n        const cfi=loc&&loc.start&&loc.start.cfi;\n        if(cfi)persist(keys.position,cfi);\n        return cfi||null;\n      }catch{return null;}\n    }\n  };`;

  let out = html.includes(navNeedle) ? html.replace(navNeedle, navPatch) : html;

  const script = `<script data-r3-audio-follow-v8="1">\n(()=>{\n  const params=new URLSearchParams(location.search);\n  const bookKey=params.get('key')||'';\n  if(!bookKey)return;\n  const audio=document.getElementById('r3AudioElement');\n  const main=document.getElementById('r3AudioMain');\n  const speed=document.getElementById('r3AudioSpeed');\n  const status=document.getElementById('r3AudioStatus');\n  const title=document.getElementById('r3AudioTitle');\n  if(!audio||!main||!status)return;\n\n  const STORAGE_KEY='r3-reader-audio-state:'+bookKey;\n  const rates=[1,1.25,1.5,1.75,2];\n  let timingWords=[];\n  let blockRanges=[];\n  let activeBlock=null;\n  let activeSignature='';\n  let currentStateId='';\n  let timingLoadSeq=0;\n  let lastSavedAt=0;\n  let lastFollowAt=0;\n  let lastFollowCfi='';\n  let restoreTime=null;\n  let restoredDirect=false;\n  let restoredSignature='';\n  let continuousBusy=false;\n\n  const delay=ms=>new Promise(resolve=>setTimeout(resolve,ms));\n  const bridge=()=>window.r3ReaderBridge||null;\n  const setStatus=text=>{status.textContent=String(text||'Nam Minh').slice(0,120);};\n\n  function normalizeText(value){\n    return String(value||'').normalize('NFC').replace(/\\r/g,'').replace(/\\u00a0/g,' ').replace(/[\\u200b-\\u200d\\u2060\\ufeff]/g,'').replace(/https?:\\/\\/\\S+/g,'').replace(/\\s+/g,' ').trim();\n  }\n  function tokenCount(value){\n    const text=normalizeText(value);\n    if(!text)return 0;\n    try{return (text.match(/[\\p{L}\\p{M}\\p{N}]+(?:['’’-][\\p{L}\\p{M}\\p{N}]+)*/gu)||[]).length;}catch{return text.split(/\\s+/).filter(Boolean).length;}\n  }\n  function framePayload(){\n    const frames=[...document.querySelectorAll('#viewer iframe')];\n    let best=null;\n    for(const frame of frames){\n      try{\n        const doc=frame.contentDocument;\n        const body=doc&&doc.body;\n        const text=String(body&&body.innerText||'').trim();\n        if(text.length<80)continue;\n        if(!best||text.length>best.text.length){\n          const heading=doc.querySelector('h1,h2,h3');\n          best={frame,doc,body,text,chapterTitle:(heading&&heading.textContent||doc.title||'').trim().slice(0,240),chapterHref:String(frame.getAttribute('src')||'').slice(0,600)};\n        }\n      }catch{}\n    }\n    if(!best)return null;\n    best.signature=best.text.length+'|'+best.text.slice(0,180)+'|'+best.text.slice(-180);\n    return best;\n  }\n  function clearHighlight(){\n    if(activeBlock){try{activeBlock.removeAttribute('data-r3-audio-reading');}catch{}}\n    activeBlock=null;\n  }\n  function ensureFrameStyle(doc){\n    if(!doc||doc.getElementById('r3AudioReadingStyle'))return;\n    const style=doc.createElement('style');\n    style.id='r3AudioReadingStyle';\n    style.textContent='[data-r3-audio-reading="1"]{font-weight:800!important}';\n    (doc.head||doc.documentElement).appendChild(style);\n  }\n  function buildBlockMap(){\n    clearHighlight();\n    blockRanges=[];\n    const payload=framePayload();\n    if(!payload||!timingWords.length)return;\n    activeSignature=payload.signature;\n    ensureFrameStyle(payload.doc);\n    const selector='p,li,blockquote,h1,h2,h3,h4,h5,h6';\n    let blocks=[...payload.doc.querySelectorAll(selector)].filter(el=>normalizeText(el.innerText||el.textContent).length>0);\n    if(!blocks.length)blocks=[...payload.body.children].filter(el=>normalizeText(el.innerText||el.textContent).length>0);\n    if(!blocks.length)blocks=[payload.body];\n    const weighted=blocks.map(el=>({el,count:Math.max(1,tokenCount(el.innerText||el.textContent))}));\n    const total=weighted.reduce((sum,row)=>sum+row.count,0)||weighted.length;\n    let cursor=0;\n    for(const row of weighted){\n      const start=Math.min(timingWords.length-1,Math.floor(cursor/total*timingWords.length));\n      cursor+=row.count;\n      const end=Math.max(start,Math.min(timingWords.length-1,Math.ceil(cursor/total*timingWords.length)-1));\n      blockRanges.push({el:row.el,start,end});\n    }\n    if(blockRanges.length)blockRanges[blockRanges.length-1].end=timingWords.length-1;\n  }\n  function wordIndexAt(ms){\n    if(!timingWords.length)return -1;\n    let lo=0,hi=timingWords.length-1,ans=0;\n    while(lo<=hi){\n      const mid=(lo+hi)>>1;\n      const start=Number(timingWords[mid]&&timingWords[mid].startMs)||0;\n      if(start<=ms){ans=mid;lo=mid+1;}else hi=mid-1;\n    }\n    return ans;\n  }\n  function blockForWord(index){\n    if(index<0)return null;\n    for(const row of blockRanges)if(index>=row.start&&index<=row.end)return row.el;\n    return blockRanges.length?blockRanges[blockRanges.length-1].el:null;\n  }\n  function blockVisible(el){\n    try{\n      const doc=el.ownerDocument;\n      const win=doc.defaultView;\n      const r=el.getBoundingClientRect();\n      const w=win&&win.innerWidth||doc.documentElement.clientWidth||1;\n      const h=win&&win.innerHeight||doc.documentElement.clientHeight||1;\n      return r.right>4&&r.left<w-4&&r.bottom>4&&r.top<h-4;\n    }catch{return true;}\n  }\n  async function followBlock(el){\n    if(!el||audio.paused||audio.ended||blockVisible(el))return;\n    const now=Date.now();\n    if(now-lastFollowAt<650)return;\n    const b=bridge();\n    if(!b||typeof b.cfiFromNode!=='function'||typeof b.display!=='function')return;\n    const cfi=b.cfiFromNode(el);\n    if(!cfi||cfi===lastFollowCfi)return;\n    lastFollowAt=now;lastFollowCfi=cfi;\n    try{await b.display(cfi);if(typeof b.persist==='function')b.persist();}catch{}\n  }\n  function syncReading(){\n    if(!timingWords.length)return;\n    const payload=framePayload();\n    if(!payload)return;\n    if(payload.signature!==activeSignature)buildBlockMap();\n    const index=wordIndexAt((Number(audio.currentTime)||0)*1000);\n    const block=blockForWord(index);\n    if(block&&block!==activeBlock){\n      clearHighlight();\n      activeBlock=block;\n      try{activeBlock.setAttribute('data-r3-audio-reading','1');}catch{}\n      followBlock(activeBlock);\n    }\n  }\n  function idFromAudio(){\n    try{\n      const raw=audio.currentSrc||audio.getAttribute('src')||'';\n      if(!raw)return '';\n      const u=new URL(raw,location.href);\n      return u.searchParams.get('id')||'';\n    }catch{return '';}\n  }\n  async function readState(id){\n    const q=new URLSearchParams({id,bookKey});\n    const response=await fetch('/artifact-library/audio?'+q.toString(),{cache:'no-store'});\n    const data=await response.json().catch(()=>({}));\n    if(!response.ok)throw new Error(data.error||('HTTP '+response.status));\n    return data;\n  }\n  async function loadTiming(state){\n    const seq=++timingLoadSeq;\n    timingWords=[];blockRanges=[];clearHighlight();\n    if(!state||!state.timingUrl)return;\n    try{\n      const response=await fetch(state.timingUrl,{cache:'no-store'});\n      const data=await response.json().catch(()=>({}));\n      if(seq!==timingLoadSeq||!response.ok||!Array.isArray(data.words))return;\n      timingWords=data.words;\n      buildBlockMap();\n      syncReading();\n    }catch{}\n  }\n  async function refreshTimingFromSource(){\n    const id=idFromAudio();\n    if(!id||id===currentStateId&&timingWords.length)return;\n    currentStateId=id;\n    try{const state=await readState(id);if(state.status==='ready')await loadTiming(state);}catch{}\n  }\n  function saveState(force=false){\n    const now=Date.now();\n    if(!force&&now-lastSavedAt<900)return;\n    const id=idFromAudio()||currentStateId;\n    const payload=framePayload();\n    if(!id||!payload)return;\n    const state={id,signature:payload.signature,chapterTitle:payload.chapterTitle||'',chapterHref:payload.chapterHref||'',time:Math.max(0,Number(audio.currentTime)||0),rate:Number(audio.playbackRate)||1,wasPlaying:!audio.paused&&!audio.ended,updatedAt:new Date().toISOString()};\n    try{localStorage.setItem(STORAGE_KEY,JSON.stringify(state));lastSavedAt=now;}catch{}\n    const b=bridge();if(b&&typeof b.persist==='function')try{b.persist();}catch{}\n  }\n  function savedState(){\n    try{const value=JSON.parse(localStorage.getItem(STORAGE_KEY)||'null');return value&&typeof value==='object'?value:null;}catch{return null;}\n  }\n  async function waitForMatchingFrame(signature,attempts=80){\n    for(let n=0;n<attempts;n++){const payload=framePayload();if(payload&&(!signature||payload.signature===signature))return payload;await delay(150);}\n    return null;\n  }\n  async function restore(){\n    const saved=savedState();\n    if(!saved||!saved.id||!Number.isFinite(Number(saved.time)))return;\n    const payload=await waitForMatchingFrame(saved.signature,100);\n    if(!payload)return;\n    try{\n      const state=await readState(saved.id);\n      if(state.status!=='ready'||!state.mediaUrl)return;\n      currentStateId=saved.id;\n      restoredSignature=saved.signature||payload.signature;\n      restoredDirect=true;\n      restoreTime=Math.max(0,Number(saved.time)||0);\n      const rate=rates.includes(Number(saved.rate))?Number(saved.rate):1;\n      audio.src=state.mediaUrl;\n      audio.playbackRate=rate;\n      if(speed)speed.textContent=rate+'×';\n      if(title&&saved.chapterTitle)title.textContent=String(saved.chapterTitle).slice(0,120);\n      await loadTiming(state);\n    }catch{}\n  }\n  async function continueToNextChapter(){\n    if(continuousBusy)return;\n    continuousBusy=true;\n    saveState(true);clearHighlight();\n    const b=bridge();\n    const before=b&&typeof b.current==='function'?b.current():null;\n    const beforeHref=before&&before.start&&before.start.href||'';\n    if(!b||typeof b.next!=='function'){setStatus('Nam Minh · đã hết chương');continuousBusy=false;return;}\n    setStatus('Nam Minh · chuyển sang chương tiếp…');\n    let changed=false;\n    for(let n=0;n<12;n++){\n      try{await b.next();}catch{}\n      await delay(160);\n      const loc=typeof b.current==='function'?b.current():null;\n      const href=loc&&loc.start&&loc.start.href||'';\n      if((beforeHref&&href&&href!==beforeHref)||(!beforeHref&&n>0)){changed=true;break;}\n    }\n    if(!changed){setStatus('Nam Minh · đã hết sách');continuousBusy=false;return;}\n    restoredDirect=false;restoredSignature='';timingWords=[];blockRanges=[];currentStateId='';\n    await delay(420);\n    const payload=await waitForMatchingFrame('',20);\n    if(payload){\n      setStatus('Nam Minh · đang chuẩn bị chương tiếp…');\n      try{main.click();}catch{}\n    }else setStatus('Nam Minh · chưa mở được chương tiếp');\n    continuousBusy=false;\n  }\n\n  main.addEventListener('click',event=>{\n    if(!restoredDirect||!audio.getAttribute('src'))return;\n    const payload=framePayload();\n    if(!payload||payload.signature!==restoredSignature){restoredDirect=false;return;}\n    event.preventDefault();event.stopImmediatePropagation();\n    if(audio.paused)audio.play().catch(()=>setStatus('Nam Minh · nhấn phát lại'));else audio.pause();\n  },true);\n\n  if(speed)speed.addEventListener('click',event=>{\n    event.preventDefault();event.stopImmediatePropagation();\n    const currentRate=Number(audio.playbackRate)||1;\n    let index=rates.findIndex(value=>Math.abs(value-currentRate)<0.01);\n    if(index<0)index=0;\n    const next=rates[(index+1)%rates.length];\n    audio.playbackRate=next;speed.textContent=next+'×';setStatus('Nam Minh · tốc độ '+next+'×');saveState(true);\n  },true);\n\n  audio.addEventListener('loadedmetadata',()=>{\n    if(restoreTime!==null){\n      const target=Math.min(Math.max(0,restoreTime),Number.isFinite(audio.duration)?Math.max(0,audio.duration-.05):restoreTime);\n      try{audio.currentTime=target;}catch{}\n      restoreTime=null;\n      setTimeout(()=>{setStatus('Nam Minh · tiếp tục từ '+Math.floor(target/60)+':'+String(Math.floor(target%60)).padStart(2,'0')+' · nhấn phát');syncReading();},0);\n    }\n    refreshTimingFromSource();\n  });\n  audio.addEventListener('timeupdate',()=>{syncReading();saveState(false);});\n  audio.addEventListener('play',()=>{saveState(true);syncReading();});\n  audio.addEventListener('pause',()=>saveState(true));\n  audio.addEventListener('ratechange',()=>saveState(true));\n  audio.addEventListener('ended',()=>{saveState(true);continueToNextChapter();});\n  audio.addEventListener('emptied',()=>{timingWords=[];blockRanges=[];clearHighlight();currentStateId='';});\n  new MutationObserver(()=>refreshTimingFromSource()).observe(audio,{attributes:true,attributeFilter:['src']});\n\n  document.addEventListener('visibilitychange',()=>{if(document.hidden)saveState(true);});\n  window.addEventListener('pagehide',()=>saveState(true));\n  window.addEventListener('beforeunload',()=>saveState(true));\n\n  setInterval(()=>{\n    const payload=framePayload();\n    if(!payload)return;\n    if(activeSignature&&payload.signature!==activeSignature&&audio.paused){clearHighlight();}\n    if(restoredDirect&&restoredSignature&&payload.signature!==restoredSignature)restoredDirect=false;\n  },1200);\n\n  restore();\n})();\n</script>`;

  out = out.replace('</body>', script + '</body>');
  return out;
}

export default {
  async fetch(request, env, ctx) {
    const url = new URL(request.url);
    const response = await app.fetch(request, env, ctx);
    if (url.pathname !== "/artifact-library/read" || request.method !== "GET") return response;
    const type = response.headers.get("Content-Type") || "";
    if (!type.toLowerCase().includes("text/html")) return response;
    const original = await response.text();
    const updated = patchContinuousAudio(original);
    const headers = new Headers(response.headers);
    headers.delete("Content-Length");
    headers.set("X-Robots-Tag", ROBOTS);
    return new Response(updated, { status: response.status, headers });
  },
  async scheduled(controller, env, ctx) {
    if (typeof app.scheduled === "function") return app.scheduled(controller, env, ctx);
  },
};