load('config.js');function abs(h){if(!h)return '';if(h.indexOf('http')===0)return h;return BASE_URL+(h.charAt(0)=='/'?h:'/'+h);}function execute(url){let r=fetch(url);if(!r.ok)return Response.error('HTTP '+r.status);let d=r.html(),out=[],seen={};d.select("a[href*='/chuong/']").forEach(e=>{let h=abs(e.attr('href'));if(!h||h.indexOf('$')>=0||seen[h])return;seen[h]=1;out.push({name:(e.text()||e.attr('title')||'Chương').trim(),url:h,host:BASE_URL,lock:false,pay:false});});return Response.success(out);}
