function execute(url){var r=fetch(url);if(!r.ok)return Response.error('HTTP '+r.status);var d=r.html();var h=d.select('h1').first();var name=h?h.text():url;var cv=d.select('.book-detail__cover img').first();var cover=cv?cv.attr('src'):'';var md=d.select("meta[name='description']").first();var desc=md?md.attr('content'):'';return Response.success({name:name,author:'',cover:cover,description:desc,detail:desc,url:url,type:'novel',format:'novel',ongoing:true});}
