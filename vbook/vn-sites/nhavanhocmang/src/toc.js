var BASE_URL='https://nhavanhocmang.com';
function execute(url){var r=fetch(url);if(!r.ok)return Response.error('HTTP '+r.status);var d=r.html(),es=d.select("a[href*='/doc/']"),out=[],seen={};for(var i=0;i<es.size();i++){var e=es.get(i),h=e.attr('href');if(!h)continue;if(h.indexOf('http')!==0)h=BASE_URL+(h.charAt(0)=='/'?h:'/'+h);if(h.indexOf(url.replace(/\/$/,'' )+'/doc/')!==0||seen[h])continue;seen[h]=1;var n=e.text()||e.attr('title')||('Chương '+(out.length+1));out.push({name:n,url:h,lock:false,pay:false});}return out.length?Response.success(out):Response.error('NO_TOC');}
