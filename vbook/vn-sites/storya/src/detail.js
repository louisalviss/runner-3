function execute(url){var r=fetch(url);if(!r.ok)return Response.error('HTTP '+r.status);var d=r.html();var h=d.select('h1').first();var name=h?h.text():url;var og=d.select("meta[property='og:image']").first();var cover=og?og.attr('content'):'';var md=d.select("meta[name='description']").first();var desc=md?md.attr('content'):'';return Response.success({name:name,author:'',cover:cover,description:desc,detail:desc,url:url,type:'novel',format:'novel',ongoing:true});}
