load('config.js');function execute(url){let r=fetch(url);if(!r.ok)return Response.error('HTTP '+r.status);let d=r.html();let n=d.select('h1').first().text()||d.select('meta[property=og:title]').attr('content');let cv=d.select('meta[property=og:image]').attr('content');let ds=d.select('meta[name=description]').attr('content');let au=d.select("a[href*='/tac-gia/']").first().text();return Response.success({name:n||url,cover:cv||'',author:au||'',description:ds||'',host:BASE_URL,ongoing:true});}
